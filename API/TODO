# API TODO - EasyJob Integration

## High Priority API Functions

### Job Information
- [ ] Query last job by barcode
  - Input: RP Barcode number
  - Output: Last job the item was assigned to
- [ ] Get jobs from project
  - List all jobs within a specific project
- [ ] Get project information details
- [ ] Get current/upcoming jobs
  - Jobs going out within 14 days

### Item & Stock Information
- [ ] Get warehouse stock count
  - Input: Item number
  - Output: Quantity currently in warehouse
- [ ] Get on-site stock count
  - Input: Item number
  - Output: Quantity currently out on jobs
- [x] Get detailed device info
  - Input: Item number
  - Output: Full device details
- [ ] Count specific items on job
  - Input: Job number, Item number
  - Output: Quantity per section on that job

### List Functions
- [x] List items on job (completed - `get_items_in_job()`)
- [ ] List items on job (non-barcoded only)
- [ ] List how many items on job by item number

## Medium Priority

### Authentication & Connection
- [x] Token-based authentication (completed)
- [x] Auto re-authentication on token expiry (completed)
- [ ] Add error handling for API failures
- [ ] Implement API response caching
- [ ] Add request retry logic

### Data Sync
- [ ] Bulk download all items from EasyJob
- [ ] Implement as scheduled task/cron job
- [ ] Sync item updates periodically
- [ ] Handle API rate limiting

## Low Priority

### Code Quality
- [ ] Add comprehensive error handling
- [ ] Implement logging system
- [ ] Add unit tests for API functions
- [ ] Document all API endpoints
- [ ] Add type hints to all functions
- [ ] Create API usage examples

### Performance
- [ ] Implement connection pooling
- [ ] Add request timeout handling
- [ ] Optimize batch requests
- [ ] Cache frequently accessed data

## Completed ✓
- [x] Basic authentication (`authenticate()`)
- [x] Quick login helper (`quick_login()`)
- [x] Get job info (`get_job_info()`)
- [x] Get device info (`get_device_info()`)
- [x] List items in job (`get_items_in_job()`)
- [x] Print items in job (`print_items_in_job()`)
- [x] Test connection (`test_connection()`)
- [x] Barcode to device ID conversion
- [x] Job number to job ID conversion
- [x] Auto re-authentication on 401 errors

## Cancelled ✘
- ✘ Load item to job (barcoded)
- ✘ Load item to job (non-barcoded)
- ✘ Unload item from job (barcoded)
- ✘ Unload item from job (non-barcoded)

## Notes
- Base URL, username, password stored in `.env` file
- SSL verification disabled for testing (self-signed certs)
- Token automatically saved to `.env` on authentication
- All requests use auto re-auth wrapper
